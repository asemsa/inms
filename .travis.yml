language: php
sudo: true
php:
  - 7.0

before_script:
  - composer install
  - composer update
  - cp .env.travis .env
  - mysql -e 'create database homestead_test;'
  - composer self-update
  - composer install --no-interaction
  

script:
  - vendor/bin/phpunit

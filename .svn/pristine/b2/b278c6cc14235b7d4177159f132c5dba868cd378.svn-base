@extends('layouts.app')
  
  @extends('coordinator.sidemenu')
  
  @section('content')

    <div class="col-sm-9 col-sm-offset-3 col-lg-10 col-lg-offset-2 main">
      <div class="row">
        <ol class="breadcrumb">
          <li>
            <a href="#">
              <em class="fa fa-home"></em>
            </a>
          </li>
          <li class="active">Assign</li>
        </ol>
      </div>
      <!--/.row-->

      <div class="row">
        <div class="col-lg-12">
          <h1 class="page-header">Assign</h1>
        </div>
      </div>
      <!--/.row-->

      <div class="row">

        <div class="col-sm-12">

          <div class="panel panel-info">
            <div class="panel-body">
				<div class="form-group">
                <label class="col-md-4 control-label" for="name">Total student for each supervisor :</label>              
                <div class="col-sm-1">
                  <input type="number" min="1" max="10" value="1">
                </div>
                  <a href="#"><button name="assign" value="assign" class="btn btn-success" type="submit"><i class="fa fa-assign"></i>Assign</button></a>
              </div>
             
              <br>

            </div>
          </div>

          <div class="panel panel-intern" style="margin-bottom: 5%;">
            <div class="panel-heading">Assign Supervisor
            </div>
            <div class="panel-body">
			 <table id="example" class="table table-striped table-bordered" cellspacing="0" width="100%">
			 <thead>
  <tr>
    <th>No</th>
    <th>Student ID</th>
    <th>Student's Name</th>
    <th>Supervisor</th>
	<th>Option</th>
  </tr>
  </thead>
   <tbody>
                @foreach($users as $i => $assign)
                  <tr>
                    <td>{{ ++$i }}</td>
                    <td>{{ $assign->matricID }}</td>
                    <td>{{ $assign->name }}</td>
                    <td>{{ $assign->name }}</td> <!-- na panggil nama supervisor-->
                    <td>
                      <form action="{{ url('/assign/delete/'.$assign->id) }}" method="post">
                        {{ csrf_field() }}
                        <a href="{{ url('/student/assign/edit/'.$assign->id) }}" class="btn btn-sm btn-warning"> <em class="fa fa-edit"></em> Edit</a>
                        {{csrf_field()}}
                        <button type="submit" class="btn btn-sm btn-danger"> <em class="fa fa-trash"></em> Delete</button>
                      </form>
                    </td>
                  </tr>
                @endforeach
              @else
                <tr>
                  <td colspan="5">No data available.</td>
                </tr>
            </tbody>
</table>

            </div><!--panelbody-->
          </div>
        </div>
      </div>

   @endsection